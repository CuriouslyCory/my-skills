## Codebase Patterns
- Monorepo uses pnpm workspaces with apps/*, packages/*, tooling/*
- Package namespace is @curiouslycory/ (renamed from @acme/)
- Turbo tasks: build, dev, typecheck, lint, test, clean, format
- Web app is at apps/web (renamed from nextjs)
- sherif postinstall lint enforces alphabetically sorted deps in package.json
- Web app build requires env vars (auth, db) - typecheck works without them
- pnpm-workspace.yaml uses catalog/catalogs for version pinning
- simple-git has strict overloads - branch on defined params rather than passing undefined
- better-sqlite3 requires native build; add to pnpm.onlyBuiltDependencies in root package.json
- When db schema changes, downstream packages (api, web) may need @types/better-sqlite3 in devDependencies for declaration emit
- Turbo caches dist/ output; after schema changes, delete .turbo + dist/ to force fresh declaration emit for downstream consumers
- FTS5 virtual tables/triggers use raw better-sqlite3 db.exec(), not Drizzle ORM; initFTS takes Database.Database instance
- CLI app (apps/cli) uses tsup for bundling, not tsc; set outDir: "dist/bin" to match bin field paths
- Import package.json in ESM with `with { type: "json" }` assertion
- CLI app needs @types/node in devDependencies for node: module imports and process global
- CLI doesn't have simple-git as direct dep; extend GitService with new methods rather than importing simple-git in CLI
- CLI commands go in apps/cli/src/commands/ and use registerXCommand(program) pattern
- @inquirer/search is a default export; source callback needs explicit type: `(input: string | undefined)`
- @inquirer/confirm and @inquirer/checkbox are default exports; use dynamic import for conditional usage
- SkillFrontmatter uses `compatibility` for agent IDs; SkillEntry (manifest) uses `agents` — don't confuse them
- smol-toml: parse(string) → object, stringify(object) → string; nested tables [a.b] = { a: { b: {} } }
- Adapters with markdown marker management reuse parseSections/buildManagedSection pattern (see CopilotAdapter, GeminiAdapter)
- Zod v4 _def doesn't reliably expose typeName; use explicit key sets instead of schema introspection for type detection
- createTRPCContext takes { headers, session } — resolve session before calling; Session type from @curiouslycory/auth
- Auth is optional: isAuthEnabled() checks ADMIN_USER env var; when disabled, getSession() returns synthetic local session
- Import drizzle-orm operators (eq, desc, etc.) from @curiouslycory/db re-exports, not directly from drizzle-orm
- Drizzle query builder types are not reassignable — use ternary or separate queries for conditional where clauses
- createTRPCContext accepts optional repoPath for filesystem operations; default is process.cwd()
- Derive DB type as `typeof db` from @curiouslycory/db/client (don't import BetterSQLite3Database directly)
- Shared lib functions go in packages/api/src/lib/ directory

# Ralph Progress Log
Started: Tue Mar 17 07:09:06 PM MST 2026
---

## 2026-03-17 19:10 - US-001
- Scaffolded monorepo by cloning create-t3-turbo template
- Moved scaffold into existing repo, preserving .claude/ and .agents/
- Renamed @acme/ to @curiouslycory/ across all files
- Removed expo and tanstack-start apps, renamed nextjs dir to web
- Created skills/ and artifacts/{agents,prompts,claudemds,compositions} dirs with .gitkeep
- Updated .gitignore to cover *.db, .my-skills/, data/
- Updated turbo.json: added test task, updated globalEnv for DB_DIALECT/DB_PATH
- Updated root package.json with turbo scripts and ralph script
- Fixed auth package.json alphabetical dep ordering (sherif lint)
- pnpm install succeeds, typecheck passes (12/12 tasks)
- **Files changed:** package.json, .gitignore, turbo.json, pnpm-workspace.yaml, pnpm-lock.yaml, apps/web/*, packages/*, tooling/*, skills/.gitkeep, artifacts/*/.gitkeep
- **Learnings for future iterations:**
  - create-turbo CLI requires interactive prompt - clone the repo directly instead
  - sherif postinstall enforces alphabetical sorting of deps - fix before committing
  - Web app build needs .env with auth vars; typecheck works without env
  - pnpm-workspace.yaml catalogs pin specific versions used across workspace
---

## 2026-03-17 - US-002
- Created @curiouslycory/shared-types package at packages/shared-types/
- Implemented Zod schemas: ArtifactCategorySchema, AgentIdSchema, SkillFrontmatterSchema, SkillEntrySchema, ManifestSchema, ConfigSchema
- Exported TypeScript types via z.infer<> for all schemas
- Exported constants: CATEGORY_DIR_MAP, DEPLOY_PATH_MAP, AGENT_NATIVE_SUPPORT
- Files changed: packages/shared-types/package.json, tsconfig.json, src/index.ts
- **Learnings for future iterations:**
  - Follow validators package pattern for simple type-only packages (zod dep, compiled-package tsconfig)
  - Use `catalog:` for zod dependency version to match workspace catalog
  - sherif alphabetical ordering applies to all package.json files including new ones
---

## 2026-03-17 - US-003
- Added gray-matter dependency to @curiouslycory/shared-types
- Created packages/shared-types/src/frontmatter.ts with parseSkillFrontmatter and buildSkillContent
- parseSkillFrontmatter uses gray-matter to parse YAML frontmatter, validates with SkillFrontmatterSchema
- buildSkillContent uses matter.stringify to produce valid SKILL.md with --- delimited YAML
- Re-exported both functions from src/index.ts
- Files changed: packages/shared-types/src/frontmatter.ts (new), packages/shared-types/src/index.ts, packages/shared-types/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - gray-matter is not in the workspace catalog; pnpm add uses latest version directly
  - gray-matter.stringify(body, data) produces valid frontmatter round-trips
  - Keep utility modules in separate files and re-export from index.ts for clean separation
---

## 2026-03-17 - US-004
- Created @curiouslycory/git-service package at packages/git-service/
- Implemented GitService class wrapping simple-git with methods: clone, fetch, pull, status, log, diff, commit, push, branches, checkout, showFileAtCommit
- clone() supports { depth: 1 } for shallow clones
- Exported simple-git types: StatusResult, LogResult, DefaultLogFields, DiffResult, BranchSummary
- Files changed: packages/git-service/package.json, tsconfig.json, src/index.ts, pnpm-lock.yaml
- **Learnings for future iterations:**
  - simple-git fetch() has multiple overloads - need to branch on which params are provided rather than passing undefined
  - simple-git is not in workspace catalog; pnpm add uses latest version directly
  - Follow existing package pattern: compiled-package tsconfig, same scripts block, same devDependencies
---

## 2026-03-17 - US-005
- Replaced Supabase/Postgres with SQLite via better-sqlite3 and drizzle-orm/better-sqlite3
- Created 5 tables: skills, variations, favorites, compositions, config (all using SQLite column types)
- client.ts reads DB_DIALECT (only sqlite supported) and DB_PATH (default: ./data/my-skills.db), auto-creates data directory
- Removed auth-schema.ts (Postgres-specific auth tables no longer needed)
- Updated index.ts exports: alias from drizzle-orm/sqlite-core instead of pg-core
- Updated drizzle.config.ts for sqlite dialect
- Added better-sqlite3 to pnpm.onlyBuiltDependencies in root package.json
- Updated downstream consumers: api post router simplified, auth adapter changed to sqlite provider, web posts component simplified
- Added @types/better-sqlite3 to api devDependencies (needed for declaration emit)
- Files changed: packages/db/package.json, src/schema.ts, src/client.ts, src/index.ts, drizzle.config.ts, src/auth-schema.ts (deleted), packages/api/package.json, packages/api/src/router/post.ts, packages/auth/src/index.ts, apps/web/src/app/_components/posts.tsx, apps/web/src/app/page.tsx, package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - better-sqlite3 needs native build scripts approved via pnpm.onlyBuiltDependencies in root package.json
  - When changing db dialect, downstream packages that emit declarations need @types for the new driver
  - Turbo aggressively caches build outputs; when schema shape changes, stale dist/.d.ts files can cause false type errors in consumers - clear .turbo + dist to force rebuild
  - SQLite uses integer timestamps with `(unixepoch())` default, not Postgres `now()`
  - drizzle-orm/better-sqlite3 `drizzle()` accepts `{ client, schema }` - no casing option needed for explicit column names
---

## 2026-03-17 - US-006
- Created packages/db/src/fts.ts with initFTS(db) function
- FTS5 virtual table `skills_fts` with columns: name, description, tags, content
- AFTER INSERT, AFTER UPDATE, AFTER DELETE triggers to keep FTS in sync with skills table
- Exported initFTS from packages/db/src/index.ts
- Files changed: packages/db/src/fts.ts (new), packages/db/src/index.ts
- **Learnings for future iterations:**
  - FTS5 virtual tables and triggers must use raw better-sqlite3 `db.exec()` since Drizzle ORM doesn't model virtual tables
  - initFTS takes the raw `Database.Database` instance, not the Drizzle wrapper
  - FTS5 UPDATE sync requires DELETE old + INSERT new pattern (no direct UPDATE on FTS5 tables)
---

## 2026-03-17 - US-007
- Scaffolded CLI app at apps/cli/ with Commander.js
- package.json: name "my-skills", type "module", engines node >=18, bin entries for "ms" and "my-skills"
- tsup.config.ts: ESM output, node18 target, shebang banner, outDir dist/bin
- src/program.ts exports createProgram() function with name, version, description
- src/bin/my-skills.ts entry point that calls createProgram().parse()
- vitest.config.ts configured with globals: true
- Dependencies: commander, chalk, ora, @inquirer/search, @inquirer/checkbox, gray-matter
- Workspace deps: @curiouslycory/shared-types, @curiouslycory/git-service
- Build produces dist/bin/my-skills.js with shebang, typecheck passes (17/17 tasks)
- Files changed: apps/cli/package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, src/bin/my-skills.ts, src/program.ts, pnpm-lock.yaml
- **Learnings for future iterations:**
  - tsup default outDir is "dist" - set outDir: "dist/bin" explicitly to match bin field path
  - CLI uses tsup (not tsc) for bundling, so tsconfig only needs base.json (not compiled-package)
  - Import package.json with `with { type: "json" }` assertion for ESM
  - tsup entry paths map to output filenames: src/bin/my-skills.ts -> dist/bin/my-skills.js
---

## 2026-03-17 - US-008
- Created apps/cli/src/core/config.ts with loadConfig() and saveConfig() functions
- loadConfig() reads ~/.my-skills/config.json, merges with defaults, validates with ConfigSchema
- saveConfig() writes atomically via .tmp file + rename
- Environment variables MY_SKILLS_CACHE_DIR and MY_SKILLS_SKILLS_DIR override file config values
- Defaults: favoriteRepos=[], cacheDir=~/.my-skills/cache, skillsDir=.agents/skills, autoDetectAgents=true, symlinkBehavior=copy
- Added @types/node to CLI devDependencies (needed for node: imports and process global)
- Files changed: apps/cli/src/core/config.ts (new), apps/cli/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - CLI app needs @types/node in devDependencies for node: module imports and process global
  - base.json tsconfig doesn't include node types by default
---

## 2026-03-17 - US-009
- Created apps/cli/src/core/manifest.ts with loadManifest, saveManifest, addSkill, removeSkill, getSkill
- loadManifest reads .my-skills.json, validates with ManifestSchema, returns null if missing
- saveManifest writes atomically via .tmp file + rename (same pattern as config.ts)
- addSkill/removeSkill/getSkill are pure functions for easy testing
- Created apps/cli/src/core/skill-hasher.ts with computeSkillHash using SHA-256
- computeSkillHash recursively collects files, sorts alphabetically for determinism, hashes filenames + contents
- Files changed: apps/cli/src/core/manifest.ts (new), apps/cli/src/core/skill-hasher.ts (new)
- **Learnings for future iterations:**
  - Follow config.ts atomic write pattern (write .tmp then rename) for any file that needs safe writes
  - Use spread + destructuring for immutable record manipulation in pure functions
  - node:crypto createHash is available without additional deps in Node 18+
---

## 2026-03-17 - US-010
- Created apps/cli/src/services/source-parser.ts with parseSource() function
- Parses three source formats: GitHub URLs, owner/repo shorthand, and local paths
- Exports SkillSource union type (GitHubSource | LocalSource)
- GitHub URL parsing handles both with and without .git suffix
- Shorthand owner/repo and owner/repo/skill-name both supported
- Local paths (./path, ../path, /path) resolved to absolute paths
- Throws descriptive error for unparseable sources
- Files changed: apps/cli/src/services/source-parser.ts (new)
- **Learnings for future iterations:**
  - Services directory (apps/cli/src/services/) is for business logic modules separate from core config/manifest
  - Source parsing uses regex for URL matching; shorthand regex excludes paths starting with . or /
---

## 2026-03-17 - US-011
- Created apps/cli/src/services/cache.ts with getCachePath, fetchRepo, isCacheStale, getCachedRepoPath, discoverSkills
- getCachePath reads cacheDir from config, returns ~/.my-skills/cache/<owner>/<repo>/
- fetchRepo does shallow clone on first fetch, fetch + resetHard("origin/HEAD") on subsequent
- Stores .my-skills-cache-meta.json with { lastFetched: ISO8601 } in each cache dir
- isCacheStale compares lastFetched against TTL (default 1 hour)
- discoverSkills recursively walks repo for SKILL.md files, parses frontmatter, returns { name, description, path }
- Added resetHard(ref?) method to GitService (packages/git-service/src/index.ts)
- Files changed: apps/cli/src/services/cache.ts (new), packages/git-service/src/index.ts
- **Learnings for future iterations:**
  - CLI doesn't have simple-git as direct dep — use GitService methods instead of importing simple-git directly
  - Added resetHard to GitService to support cache refresh workflow; extend GitService when new git operations are needed
  - discoverSkills skips node_modules and .git directories during recursive walk
---

## 2026-03-17 - US-012
- Created apps/cli/src/core/skill-resolver.ts with resolveSkill(skillName, cachePath) function
- Resolver walks cached repo recursively for SKILL.md files, matches by frontmatter name
- Returns ResolvedSkill: { name, sourcePath, frontmatter, content, files[] }
- Created apps/cli/src/core/skill-installer.ts with installSkill(skill, targetDir) function
- Installer uses fs.cp with recursive option to copy entire skill directory to targetDir/<skill-name>/
- Returns computed SHA-256 hash of installed skill via computeSkillHash
- Files changed: apps/cli/src/core/skill-resolver.ts (new), apps/cli/src/core/skill-installer.ts (new)
- **Learnings for future iterations:**
  - fs.cp with { recursive: true } is the simplest way to copy an entire directory tree
  - ResolvedSkill type is reused by skill-installer — keep the type in skill-resolver and import it
  - collectAllFiles in skill-resolver returns sorted list for deterministic output
---

## 2026-03-17 - US-013
- Created apps/cli/src/core/migration.ts with migrateFromSkillsLock(projectRoot) function
- Detects skills-lock.json when .my-skills.json is missing, returns null otherwise
- Transforms lock entries: copies source, sourceType, computedHash; adds installedAt as ISO timestamp
- Sets manifest version to 1, agents to empty array
- Writes .my-skills.json via saveManifest (atomic write), preserves skills-lock.json
- Prints informative message on successful migration
- Files changed: apps/cli/src/core/migration.ts (new)
- **Learnings for future iterations:**
  - Reuse saveManifest from manifest.ts for consistent atomic write pattern
  - skills-lock.json has simpler entries than .my-skills.json (no installedAt, no agents)
  - Use node:fs/promises access() to check file existence without reading content
---

## 2026-03-17 - US-014
- Created apps/cli/src/commands/add.ts with registerAddCommand() function
- Registered on Commander program with aliases 'a', 'i', 'install'
- ms add owner/repo/skill-name: parses source, fetches repo, resolves skill, installs to .agents/skills/<name>/, updates manifest
- ms add owner/repo (no skill name): discovers skills, presents @inquirer/search interactive picker
- Shows ora spinner during fetch and install, chalk success/error messages
- Checks manifest for existing install and suggests 'ms update' instead
- Updated apps/cli/src/program.ts to register the add command
- Files changed: apps/cli/src/commands/add.ts (new), apps/cli/src/program.ts
- **Learnings for future iterations:**
  - @inquirer/search is a default export, not a named export (use `import search from "@inquirer/search"`)
  - @inquirer/search source callback parameter needs explicit type annotation `(input: string | undefined)`
  - Commands go in apps/cli/src/commands/ directory and are registered via registerXCommand(program) pattern
---

## 2026-03-17 - US-015
- Implemented restore-from-manifest workflow in add command (ms add with no args)
- Added `restoreFromManifest()` function that iterates manifest skills, checks local hash, and re-installs from cache
- Added `sourceToGitHub()` helper to parse manifest "owner/repo" source strings into GitHubSource
- Integrates migration: if no manifest found, tries skills-lock.json migration before restore
- Shows per-skill status (installed / already up to date / failed) and summary
- Empty manifest or missing manifest prints helpful usage message
- Files changed: apps/cli/src/commands/add.ts
- **Learnings for future iterations:**
  - Manifest entry `source` field stores "owner/repo" format, needs helper to convert back to GitHubSource
  - Use computeSkillHash to compare local install against manifest computedHash for skip logic
  - node:fs/promises stat() is a clean way to check directory existence without reading content
---

## 2026-03-17 - US-016
- Implemented all add command flags: --skill, --agent, -y/--yes, -g/--global, --copy, --all, -l/--list
- --skill <names>: comma-separated skill names, bypasses interactive picker; '*' installs all skills
- --agent <agents>: comma-separated agent IDs validated against AgentIdSchema; '*' selects all agents
- -y/--yes: skips confirmation prompts; when used with owner/repo (no --skill), installs all discovered skills
- -g/--global: installs to ~/.agents/skills/ instead of project .agents/skills/
- --all: shorthand that sets --skill '*' --agent '*' -y
- -l/--list: discovers and prints available skills in a repo without installing
- Refactored single-skill install into installSingleSkill() helper for multi-skill loop
- Files changed: apps/cli/src/commands/add.ts
- **Learnings for future iterations:**
  - Commander option() with .action() passes options as second arg after the positional arg
  - AgentIdSchema.options gives the array of valid enum values for validation and wildcard expansion
  - When adding flags, keep the no-flag path identical to preserve backwards compatibility
---

## 2026-03-17 - US-017
- Created apps/cli/src/commands/remove.ts with registerRemoveCommand() function
- Registered on Commander program with aliases 'r', 'uninstall'
- ms remove <skill-name>: removes .agents/skills/<name>/ directory and manifest entry
- ms remove (no args): shows @inquirer/checkbox interactive picker of installed skills
- Supports --skill <names> for comma-separated multi-skill removal
- Supports -g/--global to remove from ~/.agents/skills/
- Supports -y/--yes to skip confirmation prompts
- Supports --all to remove all installed skills (implies -y)
- Shows error if skill is not installed
- Uses @inquirer/confirm for per-skill confirmation (dynamic import)
- Added @inquirer/confirm as dependency
- Registered in apps/cli/src/program.ts
- Files changed: apps/cli/src/commands/remove.ts (new), apps/cli/src/program.ts, apps/cli/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - @inquirer/confirm is a default export; use dynamic import when conditionally needed
  - @inquirer/checkbox returns array of selected values; set opts.yes=true after interactive selection to avoid double-confirming
  - Use fs.rm with { recursive: true, force: true } for directory removal
  - removeSkill from manifest.ts is a pure function - call saveManifest after to persist
---

## 2026-03-17 - US-018
- Created apps/cli/src/commands/list.ts with registerListCommand() function
- Registered on Commander program with alias 'ls'
- ms list reads .my-skills.json and displays installed skills as formatted chalk table (NAME, SOURCE, HASH columns)
- --global flag scans ~/.agents/skills/ directories and reads SKILL.md frontmatter
- --agent <name> flag filters to skills with matching agent in agents/compatibility array
- --json flag outputs machine-readable JSON array (no ANSI codes)
- Shows 'No skills installed' if manifest is empty or missing
- Registered in apps/cli/src/program.ts
- Files changed: apps/cli/src/commands/list.ts (new), apps/cli/src/program.ts
- **Learnings for future iterations:**
  - SkillFrontmatter uses `compatibility` field (not `agents`) for agent IDs — SkillEntry (manifest) uses `agents`
  - For global skill listing, scan directories + parse SKILL.md since there's no global manifest file
  - chalk.bold for table headers, String.padEnd for column alignment
---

## 2026-03-17 - US-019
- Created apps/cli/src/adapters/types.ts with AgentAdapter interface and AdapterSkillEntry type
- Created apps/cli/src/adapters/native-adapter.ts with NativeAdapter class (default for all agents)
- Created apps/cli/src/adapters/registry.ts with adapterRegistry Map, getAdapter(), getEnabledAdapters()
- Created apps/cli/src/adapters/index.ts barrel export
- All 10 AgentId values registered with NativeAdapter; runtime verification against AgentIdSchema.options
- Typecheck passes (17/17 tasks)
- Files changed: apps/cli/src/adapters/types.ts, native-adapter.ts, registry.ts, index.ts (all new)
- **Learnings for future iterations:**
  - Adapters directory is apps/cli/src/adapters/ with barrel index.ts
  - NativeAdapter is the default; specialized adapters (Copilot, Codex, Gemini) will replace specific entries in registry
  - AdapterSkillEntry mirrors ResolvedSkill from skill-resolver.ts — they're structurally compatible
  - Registry self-validates at import time by iterating AgentIdSchema.options
---

## 2026-03-17 - US-020
- Created apps/cli/src/adapters/detect.ts with detectAgents() and resolveAgents() functions
- detectAgents scans for marker files/directories for all 10 agent types
- resolveAgents handles first-run flow: detect → checkbox prompt → save to config + manifest
- Subsequent runs return config.defaultAgents without prompting
- Prints info message about `ms config set defaultAgents` after first selection
- Updated apps/cli/src/adapters/index.ts barrel export
- Files changed: apps/cli/src/adapters/detect.ts (new), apps/cli/src/adapters/index.ts
- **Learnings for future iterations:**
  - AGENT_MARKERS record maps each AgentId to an array of marker paths to check
  - resolveAgents checks config.defaultAgents first, then manifest.agents, then falls back to interactive detection
  - @inquirer/checkbox choices use `checked` property for pre-selection
---

## 2026-03-17 - US-021
- Created apps/cli/src/adapters/native.ts with NativeAdapter (no-op install/remove/sync since core installer handles file placement)
- Created apps/cli/src/adapters/symlink.ts with SymlinkAdapter (creates/removes symlinks from agent-specific dirs to .agents/skills/<name>/)
- Updated registry.ts to use AGENT_NATIVE_SUPPORT from shared-types, import from native.ts
- Deleted old native-adapter.ts (was redundant with core skill-installer)
- Updated index.ts barrel to export NativeAdapter from native.ts and SymlinkAdapter
- Typecheck passes (17/17 tasks)
- Files changed: apps/cli/src/adapters/native.ts (new), symlink.ts (new), registry.ts, index.ts, native-adapter.ts (deleted)
- **Learnings for future iterations:**
  - NativeAdapter is a no-op because skill-installer.ts already copies files to .agents/skills/<name>/
  - SymlinkAdapter uses AGENT_SKILLS_DIR map for per-agent symlink paths (.claude/skills/, .cursor/skills/, etc.)
  - Use lstat() not stat() to detect symlinks (stat follows symlinks, lstat doesn't)
  - symlink() with "dir" type needed for directory symlinks (important on Windows)
---

## 2026-03-17 - US-022
- Created apps/cli/src/adapters/copilot.ts implementing AgentAdapter for GitHub Copilot
- install() adds skill content between `<!-- my-skills:skill-name:start -->` and `<!-- my-skills:skill-name:end -->` markers in .github/copilot-instructions.md
- All managed content wrapped in outer `<!-- my-skills:start -->` and `<!-- my-skills:end -->` markers
- remove() removes the skill's section (markers + content); preserves everything outside managed markers
- sync() replaces entire managed section with all current skills
- detect() checks for .github/copilot-instructions.md or .github/ directory
- Creates .github/ directory and copilot-instructions.md if they don't exist
- Updated registry.ts to use CopilotAdapter for "github-copilot" instead of NativeAdapter placeholder
- Updated index.ts barrel export to include CopilotAdapter
- Typecheck passes (17/17 tasks)
- Files changed: apps/cli/src/adapters/copilot.ts (new), apps/cli/src/adapters/registry.ts, apps/cli/src/adapters/index.ts
- **Learnings for future iterations:**
  - CopilotAdapter uses HTML comment markers for section management — same pattern can be reused for any markdown-based config
  - parseSections() splits file into before/managed/after for safe content replacement
  - Registry uses direct instantiation `new CopilotAdapter()` since id/displayName are hardcoded in the class
---

## 2026-03-17 - US-023
- Created apps/cli/src/adapters/codex.ts implementing AgentAdapter for OpenAI Codex
- CodexAdapter reads/writes .codex/config.toml using smol-toml for TOML parsing
- install() adds [skills.<name>] sections with description and instructions fields
- remove() removes the [skills.<name>] section, preserving all other TOML content
- sync() replaces entire skills table with current skills
- detect() checks for .codex/config.toml or .codex/ directory
- Creates .codex/ directory and config.toml if they don't exist
- Created apps/cli/src/adapters/gemini.ts implementing AgentAdapter for Gemini CLI
- GeminiAdapter writes per-skill TOML command files to .gemini/commands/<skill-name>.toml
- GeminiAdapter manages <!-- my-skills:start/end --> markers in GEMINI.md for skill references
- remove() deletes the TOML file and removes reference from GEMINI.md
- Creates .gemini/commands/ directory and GEMINI.md if needed
- Added smol-toml as dependency for TOML parsing
- Updated registry.ts to use CodexAdapter for "codex" and GeminiAdapter for "gemini-cli"
- Updated index.ts barrel export to include CodexAdapter and GeminiAdapter
- Typecheck passes (17/17 tasks)
- Files changed: apps/cli/src/adapters/codex.ts (new), apps/cli/src/adapters/gemini.ts (new), apps/cli/src/adapters/registry.ts, apps/cli/src/adapters/index.ts, apps/cli/package.json, package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - smol-toml has simple API: parse(string) → object, stringify(object) → string
  - TOML nested tables like [skills.foo] map to nested objects: { skills: { foo: { ... } } }
  - GeminiAdapter uses dual storage: per-skill TOML files + GEMINI.md reference list
  - Same parseSections/buildManagedSection pattern from CopilotAdapter reused for GEMINI.md marker management
---

## 2026-03-17 - US-024
- Integrated adapter system into add and remove commands
- add command: after installing a skill, resolves agents (via --agent flag or resolveAgents() detection flow) and runs adapter.install() for each enabled agent
- remove command: after removing skill files, runs adapter.remove() for each enabled agent
- Added runAdapterInstalls() helper in add.ts and runAdapterRemoves() helper in remove.ts
- Adapter failures are logged as warnings (chalk.yellow) but don't fail the overall command
- Agents list stored in manifest entry for each installed skill
- Logs deployed/removed adapters (e.g., "Deployed to: Claude Code, Cursor, GitHub Copilot")
- Files changed: apps/cli/src/commands/add.ts, apps/cli/src/commands/remove.ts
- **Learnings for future iterations:**
  - ResolvedSkill from skill-resolver.ts is structurally compatible with AdapterSkillEntry — no conversion needed
  - resolveAgents() handles first-run detection flow; only call when --agent flag not provided
  - Use `const resolvedAgents: AgentId[] = agents` pattern to narrow `AgentId[] | undefined` after conditional assignment
  - Adapter failures should always be warnings, not errors — keeps the primary install/remove operation from failing
---

## 2026-03-17 - US-025
- Created apps/cli/src/commands/update.ts with registerUpdateCommand() function
- Registered on Commander program with alias 'up'
- ms update (no args) updates all installed skills from manifest
- ms update <skill-name> updates a specific skill
- For each skill: force-fetches latest from remote via cache, resolves skill, compares hash
- If hash differs: removes old files, installs new version, re-runs adapter install, updates manifest entry (hash, installedAt)
- If hash matches: prints 'skill-name: already up to date'
- Shows summary: N updated, N already up-to-date, N failed
- Supports -g/--global flag for ~/.agents/skills/
- Files changed: apps/cli/src/commands/update.ts (new), apps/cli/src/program.ts
- **Learnings for future iterations:**
  - Update command reuses sourceToGitHub and runAdapterInstalls patterns from add command — these could be extracted to shared utils if more commands need them
  - entry.agents stores AgentId[] but typed as string[] in schema — needs `as AgentId[]` cast
  - Use rm with { recursive: true, force: true } before reinstall to ensure clean state
---

## 2026-03-17 - US-026
- Created apps/cli/src/commands/check.ts with registerCheckCommand() function
- Registered on Commander program (alphabetically between add and list)
- ms check reads manifest, for each skill: force-fetches latest from remote, resolves skill, computes hash of remote source
- Displays formatted table with columns: NAME, STATUS (up-to-date/update available/remote unavailable), CURRENT (8-char hash), LATEST (8-char hash)
- Status colored: green for up-to-date, yellow for update available, red for remote unavailable
- Shows spinner during fetch operations with per-skill progress text
- Shows 'No skills installed' if manifest is empty
- Non-github source types and fetch failures gracefully show "remote unavailable"
- Files changed: apps/cli/src/commands/check.ts (new), apps/cli/src/program.ts
- **Learnings for future iterations:**
  - computeSkillHash can hash any directory, not just installed skills — useful for comparing remote source directories against manifest hash
  - sourceToGitHub helper is duplicated across update.ts and check.ts — candidate for extraction to shared utils
  - Check command is read-only: no manifest writes, no file changes, no adapter calls
---

## 2026-03-17 - US-027
- Created apps/cli/src/commands/find.ts with registerFindCommand() function
- Registered on Commander program with alias 'f'
- ms find (no query) launches @inquirer/search interactive fuzzy-find across all indexed skills
- ms find <query> pre-fills the search with the query string
- Search index built from: installed skills (manifest) + all skills from cached favorite repos
- Favorite repos fetched if not cached or cache is stale (1 hour TTL via isCacheStale)
- Each result shows: skill name, description, source repo, '[installed]' badge if already installed
- Fuzzy match filters against name and description
- Selecting an uninstalled result prompts 'Install skill-name from owner/repo?' via @inquirer/confirm
- Delegates to add command via program.parseAsync for actual installation
- Reads favoriteRepos from global config (~/.my-skills/config.json)
- Files changed: apps/cli/src/commands/find.ts (new), apps/cli/src/program.ts
- **Learnings for future iterations:**
  - program.parseAsync(["add", source], { from: "user" }) delegates to existing commands cleanly
  - favoriteToGitHub reuses parseSource from source-parser.ts to validate favorite repo strings
  - getCachedRepoPath returns null when repo is not cached; isCacheStale checks TTL from cache metadata
---

## 2026-03-17 - US-028
- Created apps/cli/src/commands/init.ts with registerInitCommand() function
- Registered on Commander program (alphabetically between find and list)
- ms init <name> creates .agents/skills/<name>/SKILL.md with frontmatter template via buildSkillContent
- ms init (no name) prompts for name and description using @inquirer/input (dynamic import)
- Generated SKILL.md has --- delimited YAML frontmatter with name, description, plus placeholder body
- Creates directory recursively if it doesn't exist
- Shows error if skill already exists with suggestion to use --force
- --force flag overwrites existing skill
- Added @inquirer/input as dependency
- Typecheck passes (17/17 tasks)
- Files changed: apps/cli/src/commands/init.ts (new), apps/cli/src/program.ts, apps/cli/package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - @inquirer/input is a default export, same pattern as @inquirer/confirm and @inquirer/search
  - buildSkillContent from shared-types generates proper frontmatter SKILL.md content
  - Use node:fs/promises access() to check file existence for error-before-overwrite pattern
---

## 2026-03-17 - US-029
- Created apps/cli/src/commands/config.ts with registerConfigCommand() function
- Registered on Commander program with subcommands: get, set, list, delete
- ms config get <key>: reads and prints a config value (supports dot-notation for nested values)
- ms config set <key> <value>: sets a value, validates with ConfigSchema, saves
- ms config set defaultAgents claude-code,cursor handles comma-separated arrays for array fields
- ms config list: pretty-prints all config key-value pairs with chalk formatting
- ms config delete <key>: resets key to DEFAULT_CONFIG value and saves
- Array keys (defaultAgents, favoriteRepos) and boolean keys (autoDetectAgents) parsed from string input
- All operations read from / write to ~/.my-skills/config.json via loadConfig/saveConfig
- Shows current value after set operations with green checkmark
- Typecheck passes (17/17 tasks)
- Files changed: apps/cli/src/commands/config.ts (new), apps/cli/src/program.ts
- **Learnings for future iterations:**
  - Zod v4 _def shape doesn't have typeName on all defs; use explicit key sets (ARRAY_KEYS, BOOLEAN_KEYS) instead of introspecting schema
  - Commander subcommands: use program.command("config") then configCmd.command("get <key>") for nested commands
  - DEFAULT_CONFIG is exported from core/config.ts and useful for the "delete" (reset to default) operation
---

## 2026-03-17 - US-030
- Updated apps/web/src/env.ts: removed POSTGRES_URL (no longer needed with SQLite), added REPO_PATH (optional, defaults to process.cwd())
- Added @curiouslycory/shared-types and @curiouslycory/git-service as workspace dependencies to apps/web
- tRPC client was already configured from create-t3-turbo scaffold connecting to @curiouslycory/api
- Web app already renamed from nextjs to web with correct package name @curiouslycory/web
- Typecheck passes (17/17 tasks)
- Files changed: apps/web/package.json, apps/web/src/env.ts, package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - The create-t3-turbo scaffold already has tRPC fully wired (client, server RSC, API route handler) — just add deps and fix env
  - REPO_PATH env var is optional in env.ts; use `env.REPO_PATH ?? process.cwd()` in routers that need filesystem access
  - Web app env.ts extends authEnv() which requires AUTH_DISCORD_ID/SECRET — skip validation with CI=true or skipValidation for typecheck
---

## 2026-03-17 - US-031
- Replaced Better Auth with simple cookie-based JWT auth using jose library
- packages/auth/src/index.ts: exports validate(), createSession(), verifySession(), isAuthEnabled(), Session type
- validate() checks against ADMIN_USER and ADMIN_PASSWORD env vars
- createSession() returns HS256 JWT signed with AUTH_SECRET (7-day expiry)
- verifySession() validates JWT and returns Session | null
- isAuthEnabled() returns true only when ADMIN_USER is set
- Updated authEnv() to use optional ADMIN_USER/ADMIN_PASSWORD/AUTH_SECRET (removed Discord vars)
- Removed Better Auth dependencies from auth package and web app
- Updated createTRPCContext to accept session directly instead of auth object
- Updated web app consumers: trpc/server.tsx, api/trpc route, auth/server.ts, auth-showcase component
- Removed Better Auth catch-all route (apps/web/src/app/api/auth/[...all]/route.ts)
- Removed Better Auth client (apps/web/src/auth/client.ts)
- Removed Better Auth CLI script (packages/auth/script/auth-cli.ts)
- Deleted @curiouslycory/db dependency from auth package (no longer needed)
- Typecheck passes (17/17 tasks)
- Files changed: packages/auth/src/index.ts, packages/auth/env.ts, packages/auth/package.json, packages/auth/script/auth-cli.ts (deleted), packages/api/src/trpc.ts, apps/web/package.json, apps/web/src/auth/server.ts, apps/web/src/auth/client.ts (deleted), apps/web/src/trpc/server.tsx, apps/web/src/app/api/trpc/[trpc]/route.ts, apps/web/src/app/api/auth/[...all]/route.ts (deleted), apps/web/src/app/_components/auth-showcase.tsx, pnpm-lock.yaml
- **Learnings for future iterations:**
  - createTRPCContext now takes { headers, session } instead of { headers, auth } — all tRPC context providers must resolve session before calling
  - Auth is optional: when ADMIN_USER is not set, getSession() returns a synthetic { user: { username: "local" } } session
  - jose library: SignJWT().setProtectedHeader().setIssuedAt().setExpirationTime().sign() for creation; jwtVerify(token, secret) for verification
  - Web app auth/server.ts uses SESSION_COOKIE="my-skills-session" cookie name for JWT token storage
---

## 2026-03-17 - US-032
- Created packages/api/src/router/skill.ts with full CRUD + syncFromDisk
- skill.list: query with optional category and tags filters, ordered by updatedAt desc
- skill.byId: query returning single skill by ID
- skill.create: protected mutation writing SKILL.md to disk + inserting into DB
- skill.update: protected mutation updating SKILL.md on disk + DB record
- skill.delete: protected mutation removing skill directory + DB record
- skill.syncFromDisk: protected mutation scanning skills/ directory, upserting to DB, removing stale entries
- Added repoPath to tRPC context (from REPO_PATH env var or cwd fallback)
- Updated web app tRPC context providers (API route + RSC) to pass repoPath
- Added @curiouslycory/shared-types and gray-matter as dependencies to api package
- Registered skill router in app router (packages/api/src/root.ts)
- Typecheck passes (17/17 tasks)
- Files changed: packages/api/src/router/skill.ts (new), packages/api/src/root.ts, packages/api/src/trpc.ts, packages/api/package.json, apps/web/src/app/api/trpc/[trpc]/route.ts, apps/web/src/trpc/server.tsx, pnpm-lock.yaml
- **Learnings for future iterations:**
  - Import drizzle-orm operators (eq, desc) from @curiouslycory/db re-exports, not directly from drizzle-orm (module resolution fails for direct imports)
  - Drizzle query builder types are not reassignable (let query = ... then query = query.where()) — use ternary or separate queries instead
  - createTRPCContext now accepts optional repoPath; all context providers should pass it from REPO_PATH env var
  - buildSkillContent and parseSkillFrontmatter are exported from @curiouslycory/shared-types for disk read/write operations
---

## 2026-03-17 - US-033
- Created packages/api/src/router/artifact.ts with full CRUD + syncFromDisk for artifacts
- artifact.list: query with optional category filter, returns only artifact categories (agent/prompt/claudemd)
- artifact.listByCategory: query filtering by specific artifact category
- artifact.byId: query returning single artifact by ID
- artifact.create: protected mutation writing SKILL.md to artifacts/<category>/<name>/ on disk + inserting into DB
- artifact.update: protected mutation updating SKILL.md on disk + DB record
- artifact.delete: protected mutation removing artifact directory + DB record
- artifact.syncFromDisk: protected mutation scanning all artifacts/ subdirectories (agents, prompts, claudemds), upserting to DB, removing stale entries
- Registered artifact router in app router (packages/api/src/root.ts)
- Reuses skills DB table with category field for differentiation
- Uses CATEGORY_DIR_MAP from shared-types for category-to-directory mapping
- Typecheck passes (17/17 tasks)
- Files changed: packages/api/src/router/artifact.ts (new), packages/api/src/root.ts
- **Learnings for future iterations:**
  - Artifacts reuse the skills DB table — category field distinguishes skill vs agent/prompt/claudemd
  - CATEGORY_DIR_MAP maps category to subdirectory name (agent→agents, prompt→prompts, claudemd→claudemds)
  - syncFromDisk stale removal must filter by artifact categories to avoid deleting skill entries
  - Artifact router follows identical pattern to skill router — same walkDirs helper, same CRUD structure
---

## 2026-03-17 - US-034
- Created packages/api/src/lib/disk-sync.ts with scanAndSync(repoPath, db) function
- scanAndSync walks skills/*/SKILL.md and artifacts/<category>/*/SKILL.md, parsing frontmatter
- Upserts each found item into skills DB table matched on dirPath
- Removes stale DB records whose dirPath no longer exists on disk
- Returns summary: { added, updated, removed }
- Refactored skill.syncFromDisk and artifact.syncFromDisk to both delegate to scanAndSync
- Exported ScanResult interface for declaration emit compatibility
- Typecheck passes (17/17 tasks)
- Files changed: packages/api/src/lib/disk-sync.ts (new), packages/api/src/router/skill.ts, packages/api/src/router/artifact.ts
- **Learnings for future iterations:**
  - Cannot import BetterSQLite3Database type directly from drizzle-orm/better-sqlite3 (no type declarations available in this workspace); derive DB type from `typeof db` instead
  - When a shared function returns a type not explicitly exported, declaration emit fails with "cannot be named" error — export the return type interface
  - The scanAndSync function handles both skills and artifacts in a single pass, removing all stale entries regardless of category
---

## 2026-03-17 - US-035
- Created packages/api/src/router/search.ts with search.query procedure
- search.query accepts query string, optional category filter, limit (default 20), offset
- Empty query returns recent items ordered by updatedAt desc
- Non-empty query builds FTS5 MATCH with prefix matching (each term quoted and appended with *)
- Returns results with highlighted snippets via FTS5 snippet() function with `<mark>` tags
- Results ordered by FTS5 rank for relevance
- Registered search router in app router (packages/api/src/root.ts)
- Added initFTS(sqlite) call to packages/db/src/client.ts so FTS tables are created at startup
- Typecheck passes (17/17 tasks)
- Files changed: packages/api/src/router/search.ts (new), packages/api/src/root.ts, packages/db/src/client.ts
- **Learnings for future iterations:**
  - Drizzle better-sqlite3 wrapper exposes db.all<T>(sql`...`) for raw queries returning typed rows
  - FTS5 snippet() takes: table, column_index (-1 for best), open_tag, close_tag, ellipsis, max_tokens
  - FTS5 MATCH terms should be quoted to handle special chars; append * for prefix matching
  - initFTS must be called before any FTS queries — adding to client.ts ensures it runs at module load time
  - Raw SQL results use snake_case column names (dir_path, created_at) — map to camelCase in return
---
